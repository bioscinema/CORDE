% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paired_sodt_test.R
\name{paired_sodt_test}
\alias{paired_sodt_test}
\title{Paired Sum-of-Distances Test (SoDT) for Paired Dissimilarity Matrices}
\usage{
paired_sodt_test(D0, D1, Y, nperm = 999, seed = 2025)
}
\arguments{
\item{D0}{Numeric matrix. Symmetric dissimilarity for condition 0 with zeros on the diagonal.}

\item{D1}{Numeric matrix. Symmetric dissimilarity for condition 1 with zeros on the diagonal.}

\item{Y}{Factor or character vector of length n giving the group label for each sample.}

\item{nperm}{Integer. Number of permutations (default = 999).}

\item{seed}{Integer. Random seed (default = 2025).}
}
\value{
A data.frame with rows for Between-group (ΔSSB), Within-group (ΔSSW), PERMANOVA F, and per-group ΔSSW.
Columns: \code{Source}, \code{Delta}, \code{P_value}.
}
\description{
Performs a paired Sum-of-Distances Test to compare group-level dispersion and separation
between two symmetric dissimilarity matrices (e.g., pre vs post), accounting for within-group variability.
Empirical p-values are obtained via two permutation schemes: group-label shuffling and within-sample swapping.
}
\details{
Computes centered Gram matrices G = -1/2 * J D^2 J, projects onto the group hat-matrix,
then uses Rcpp-accelerated permutations (between-group shuffles and paired swaps).
P-values use (1 + #{|Δ_perm| >= |Δ_obs|}) / (nperm + 1).
}
